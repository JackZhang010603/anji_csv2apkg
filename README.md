# anji_csv2apkg

Convert CSV files to Anki flashcards (.apkg)

## Usage

1. Put CSV files in `input/` folder
2. Run: `python csv2apkg.py`
3. Get .apkg files from `output/` folder

## CSV Format
```
word,meaning
apple,red fruit
book,reading material
```

## Requirements
```
pip install genanki
```